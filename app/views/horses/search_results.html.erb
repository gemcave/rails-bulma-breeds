<p>
	Your search for '<%= @query %>' returned <%= pluralize(@horses.size,'result') %>.
</p>

<div class="columns is-multiline">
	<%= render 'horse',
						 collection: @horses,
						 locals: {name_is_link: true} %>
</div>